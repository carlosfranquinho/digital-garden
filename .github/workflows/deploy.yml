name: Process and Deploy Digital Garden

on:
  repository_dispatch:
    types: [obsidian-update]

jobs:
  process-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Baixar o digital-garden (limpo)
      - name: 🚀 Checkout Limpo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          path: digital-garden

      # 2. Clonar Obsidian Vault
      - name: 📥 Clonar Vault
        run: |
          git clone https://${{ github.actor }}:${{ secrets.GARDEN_TOKEN }}@github.com/${{ github.repository_owner }}/obsidian-vault.git /tmp/obsidian-vault
          echo '${{ toJSON(github.event.client_payload.notas) }}' > /tmp/notas.json

      # 3. Limpeza Radical
      - name: 💥 Limpar Tudo
        run: |
          cd digital-garden
          # Mantém apenas .git, .github e config.toml
          find . -mindepth 1 \( -name '.git' -o -name '.github' -o -name 'config.toml' \) -prune -o -exec rm -rf {} +
          mkdir -p content/posts static/imagens

      # 4. Processar Arquivos
      - name: 📂 Copiar Conteúdo
        run: |
          cd digital-garden
          
          slugify() {
            echo "$1" | 
            iconv -t ascii//TRANSLIT |
            tr -d "'\"" |
            tr -s '[:space:][:punct:]' '-' |
            tr '[:upper:]' '[:lower:]' |
            sed 's/-$//;s/^-//;s/[^a-z0-9\-]//g'
          }

          while IFS= read -r original; do
            if [[ "$original" == *.md ]]; then
              novo_nome="content/posts/$(slugify "$original")"
              mkdir -p "$(dirname "$novo_nome")"
              cp "/tmp/obsidian-vault/$original" "$novo_nome"
              sed -i -E 's|!\[\[(attachments/)?([^]]+)\]\]|![](/imagens/\2)|g' "$novo_nome"
              echo "📄 $original → $novo_nome"

            elif [[ "$original" == attachments/* ]]; then
              cp "/tmp/obsidian-vault/$original" "static/imagens/$(basename "$original")"
              echo "🖼️ $(basename "$original")"
            fi
          done < <(jq -r '.[]' /tmp/notas.json)

      # 5. Verificar Estrutura (DEBUG)
      - name: 🔍 Verificar Arquivos
        run: |
          cd digital-garden
          echo "=== Estrutura Final ==="
          find . -type f
          echo "=== config.toml ==="
          cat config.toml || echo "Arquivo config.toml não encontrado!"

      # 6. Build com Hugo
      - name: 🛠️ Instalar Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: 'latest'
          extended: true

      - name: 📦 Gerar Site
        run: |
          cd digital-garden
          hugo --minify --source .

      # 7. Deploy FTP
      - name: 📡 FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ${{ secrets.FTP_PATH }}
          local-dir: digital-garden/public/
